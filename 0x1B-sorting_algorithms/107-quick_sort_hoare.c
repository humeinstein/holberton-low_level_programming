#include "sort.h"
/**
 * quick_sort_hoare - quick sort with hoare partition.
 * @array: array of integers.
 * @size: size of array.
 */
void quick_sort_hoare(int *array, size_t size)
{
	if (!array || size < 2)
		return;
}
