#include "sort.h"
/**
 * bitonic_sort - bitonic sort algorithm.
 * @array: array of integers.
 * @size: size of array.
 */
void bitonic_sort(int *array, size_t size)
{
	if (!array || size < 2)
		return;
}
