#include "sort.h"
/**
 * radix_sort - radix sort algorithm.
 * @array: array of integers.
 * @size: size of array.
 */
void radix_sort(int *array, size_t size)
{
	if (!array || size < 2)
		return;
}
